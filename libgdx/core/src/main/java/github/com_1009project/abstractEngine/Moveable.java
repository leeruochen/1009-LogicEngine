package github.com_1009project.abstractEngine;

// The Moveable interface defines a contract for any entity that can move in the game.

public interface Moveable {
	public void updateMovement(float delta);
}
